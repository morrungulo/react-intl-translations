const PORT = 3001
const URL = 'http://localhost'

export const URL_GET_FILES = `${URL}:${PORT}`

export const URL_LOAD_FILE = `${URL}:${PORT}/load`

export const URL_SAVE_FILE = `${URL}:${PORT}/save`

export const DRAWER_WIDTH = 320
